node_modules/
dist/
.next/
workspace/
# root-anchored: bare "releases/" would also ignore route segments like
# apps/web/src/app/api/projects/[projectId]/releases/
/releases/
apps/web/releases/
audit.log.jsonl
*.tsbuildinfo
.env
.env.*
!.env.example
.DS_Store
coverage/

# local design-preview artifacts
.playwright-mcp/
theme-*.png
shot-*.png

# local quickstart/demo scratch outputs (graph + manifest run artifacts)
/graph*.json
/manifest*.json
.gemini/

# encrypted credential vault + any local audit logs (never commit secrets)
vault.json
demo/**/vault.json
demo/**/audit.log.jsonl

# local recorder/TTS tooling (ffmpeg-static, piper binary + voice model — large)
demo/recorder/
